apiVersion: v1        # versi API
kind: Service         # jenis obyek kubernetes
metadata:
  name: karsajobs-service
  labels:
    app: backend
spec:
  type: NodePort      # tipe servis
  ports:              
  - port: 8080        # port yg dipakai
  selector:           # Pemilih pod yang diservis
    app: backend